<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title></title>
    <style>
        body{
            text-align: center;
            margin: auto;
            width: 60vw;
            margin-top: 10vh;
        }

        ::placeholder{ color:#d2d2d2 }

        form input:nth-child(1){
            transition: all 0.5s;
            cursor: text;
            padding-bottom: 10px;
            border: none;
            outline: none;
            border-bottom: 1.5px solid #d2d2d2;
            width: 100%; 
            font-size: 1em;
            margin-top: 1vh;
        }
        form input:nth-child(1):focus{
            border-bottom: 1.5px solid #26a69a;
        }

        form input:nth-child(2){
            outline: none;
            font-size: 0.8em;
            letter-spacing: 0.5px;
            cursor: pointer;
            transition: all 0.5s;
            margin-top: 20px;
            background-color: #26a69a;
            color: white;
            border-radius: 4px;
            padding: 8px 20px;
            box-shadow: 1px 2px 2px 0 rgba(0,0,0,0.14), 0 3px 1px -2px rgba(0,0,0,0.12), 0 1px 5px 0 rgba(0,0,0,0.2);
        }
        form input:nth-child(2):hover{
            background-color:#53cfc3; 
        }
    </style>
</head>
<body>
    <form action="/weather" method="POST">
        <input type="text" name="cityName" placeholder="Enter a City">
        <input type="submit" value="SUBMIT">
    </form>
</body>
</html>